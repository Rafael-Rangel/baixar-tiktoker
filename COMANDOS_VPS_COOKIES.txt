# 🔐 COMANDOS PARA COPIAR COOKIES PARA VPS

## ✅ Arquivo de cookies verificado:
- Tamanho: 666KB (666.000 bytes)
- Linhas: 2035
- Cookies do YouTube: 361
- Formato: Netscape HTTP Cookie File ✓

## 📋 Opção 1: Via SCP (Recomendado)

### Na sua máquina local:
```bash
scp ~/Área\ de\ trabalho/Projetos/workflow_multivideos/cookies.txt root@SEU_IP_VPS:~/content-orchestrator/data/cookies.txt
```

### Na VPS:
```bash
# Verificar se o arquivo foi copiado
ls -lh ~/content-orchestrator/data/cookies.txt

# Ajustar permissões
chown 1000:1000 ~/content-orchestrator/data/cookies.txt
chmod 644 ~/content-orchestrator/data/cookies.txt

# Verificar dentro do container
docker exec content-orchestrator ls -lh /app/data/cookies.txt

# Reiniciar container
docker compose restart content-orchestrator

# Verificar logs
docker logs -f content-orchestrator
```

## 📋 Opção 2: Via Edição Manual (Copiar/Colar)

### Na VPS:
```bash
# Criar/editar arquivo
nano ~/content-orchestrator/data/cookies.txt
```

### Cole TODO o conteúdo do arquivo cookies.txt local
- Pressione Ctrl+Shift+V ou botão direito → Paste
- Salve: Ctrl+X, Y, Enter

### Depois execute:
```bash
# Ajustar permissões
chown 1000:1000 ~/content-orchestrator/data/cookies.txt
chmod 644 ~/content-orchestrator/data/cookies.txt

# Verificar tamanho (deve ser ~666KB)
ls -lh ~/content-orchestrator/data/cookies.txt

# Verificar dentro do container
docker exec content-orchestrator ls -lh /app/data/cookies.txt
docker exec content-orchestrator head -5 /app/data/cookies.txt

# Reiniciar container
docker compose restart content-orchestrator

# Monitorar logs
docker logs -f content-orchestrator
```

## ✅ Verificação Final

Após copiar e reiniciar, verifique:

```bash
# Tamanho do arquivo (deve ser ~666KB, não 1619 bytes)
docker exec content-orchestrator ls -lh /app/data/cookies.txt

# Primeiras linhas (deve começar com "# Netscape HTTP Cookie File")
docker exec content-orchestrator head -5 /app/data/cookies.txt

# Contar cookies do YouTube (deve ter centenas)
docker exec content-orchestrator grep -c "^\.youtube\.com" /app/data/cookies.txt
```

## 🧪 Testar Download

Após reiniciar, teste o download novamente pelo n8n. O erro "Sign in to confirm you're not a bot" deve desaparecer.
