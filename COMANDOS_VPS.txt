═══════════════════════════════════════════════════════════════
COMANDOS PARA DEPLOY NA VPS - TUDO PRONTO NO GITHUB
═══════════════════════════════════════════════════════════════

📋 PASSO A PASSO COMPLETO
───────────────────────────────────────────────────────────────

1. REMOVER INSTALAÇÃO ANTIGA
───────────────────────────────────────────────────────────────
cd ~
rm -rf tiktok-downloader-api

2. CLONAR REPOSITÓRIO ATUALIZADO
───────────────────────────────────────────────────────────────
git clone https://github.com/Rafael-Rangel/baixar-tiktoker.git tiktok-downloader-api
cd tiktok-downloader-api

3. CONFIGURAR SESSÃO INICIAL (RESOLVER CLOUDFLARE MANUALMENTE)
───────────────────────────────────────────────────────────────
# Se você tem acesso gráfico (X11/VNC):
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
playwright install chromium
python setup_session.py oprimorico

# Seguir instruções:
# - Resolver desafio Cloudflare manualmente no navegador
# - Aguardar página carregar
# - Pressionar ENTER
# - Cookies serão salvos automaticamente

4. BUILD E DEPLOY DOCKER
───────────────────────────────────────────────────────────────
cd ~
docker compose build tiktok-downloader-api
docker compose up -d tiktok-downloader-api

5. VERIFICAR LOGS
───────────────────────────────────────────────────────────────
docker logs -f tiktok-downloader-api

6. TESTAR
───────────────────────────────────────────────────────────────
curl -X POST http://localhost:5000/channels/latest \
  -H "Content-Type: application/json" \
  -d '{"channels": ["oprimorico"]}'

═══════════════════════════════════════════════════════════════
✅ TUDO SALVO NO GITHUB E PRONTO PARA USO!
═══════════════════════════════════════════════════════════════
