# COMANDOS PARA REBUILD FORÇADO (SEM CACHE)

# O container está usando código antigo. Execute estes comandos na VPS:

cd ~
docker compose stop content-orchestrator
docker compose rm -f content-orchestrator
docker compose build content-orchestrator --no-cache
docker compose up -d content-orchestrator
sleep 5
docker logs --tail 20 content-orchestrator

# Verificar se o código está atualizado:
docker exec content-orchestrator head -30 /app/app/api/routes/n8n.py

# O código novo NÃO deve ter "BackgroundTasks" na linha 6
# O código novo deve ter apenas "from fastapi import APIRouter"
