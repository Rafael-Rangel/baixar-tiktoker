# Exemplo: usar Content Orchestrator na mesma rede do n8n
# 1. Adicione este serviço ao docker-compose do n8n, OU
# 2. docker compose -f docker-compose.yml -f docker-compose.n8n.example.yml up -d
# n8n chama: http://content-orchestrator:8000

services:
  content-orchestrator:
    networks:
      - root_default

networks:
  root_default:
    external: true
