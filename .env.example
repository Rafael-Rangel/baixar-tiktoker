# Content Orchestrator - Variáveis de ambiente
# cp .env.example .env e ajuste se necessário

PROJECT_NAME=Content Orchestrator
API_V1_STR=/v1
STORAGE_TYPE=local

# Docker: /content-downloads para bater com n8n (rm -rf /content-downloads/<grupo>)
LOCAL_STORAGE_PATH=/content-downloads
DATA_PATH=/app/data
