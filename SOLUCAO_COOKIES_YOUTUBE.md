# 🔧 Solução para Bloqueio do YouTube

## Problema Atual

O YouTube está bloqueando downloads com erro: **"Sign in to confirm you're not a bot"**

Isso acontece mesmo com cookies porque:
1. Arquivo de cookies pode estar incompleto (1619 bytes é muito pequeno)
2. Cookies podem estar expirados
3. YouTube pode estar bloqueando por rate limiting ou detecção de bot

## Soluções

### 1. Restaurar Arquivo de Cookies Original

O arquivo original tinha **77006 bytes**. Verifique se ainda existe:

```bash
# Na VPS, verificar tamanhos
ls -lh ~/cookies.txt
ls -lh ~/content-orchestrator/data/cookies.txt

# Se o arquivo original (~/cookies.txt) tiver 77006 bytes, restaurar:
cp ~/cookies.txt ~/content-orchestrator/data/cookies.txt
chown 1000:1000 ~/content-orchestrator/data/cookies.txt
chmod 644 ~/content-orchestrator/data/cookies.txt
docker compose restart content-orchestrator
```

### 2. Exportar Novos Cookies

Se o arquivo original também estiver pequeno, exporte novos cookies:

**Chrome/Edge:**
1. Instale extensão: "Get cookies.txt LOCALLY" ou "cookies.txt"
2. Acesse https://www.youtube.com (faça login)
3. Clique na extensão → Export → Salve como `cookies.txt`
4. Copie o arquivo para a VPS

**Firefox:**
1. Instale extensão: "cookies.txt"
2. Acesse https://www.youtube.com (faça login)
3. Clique na extensão → Export
4. Copie o arquivo para a VPS

**Na VPS:**
```bash
# Copiar arquivo (via SCP ou editar diretamente)
nano ~/content-orchestrator/data/cookies.txt
# Cole o conteúdo, salve (Ctrl+X, Y, Enter)

# Ajustar permissões
chown 1000:1000 ~/content-orchestrator/data/cookies.txt
chmod 644 ~/content-orchestrator/data/cookies.txt
docker compose restart content-orchestrator
```

### 3. Verificar Formato do Arquivo

O arquivo deve estar no formato Netscape e começar com:
```
# Netscape HTTP Cookie File
# This file is generated by yt-dlp.  Do not edit.

.youtube.com    TRUE    /       TRUE    ...
```

### 4. Testar com Outro Vídeo

O vídeo `YPD6K509zfI` pode ter restrições específicas. Teste com outro vídeo para verificar se o problema é geral ou específico.

### 5. Aguardar Rate Limiting

Se for rate limiting, aguarde 15-30 minutos e tente novamente.

## Verificação

Após restaurar/atualizar cookies, verifique:

```bash
# Verificar tamanho (deve ser > 10KB para ser completo)
ls -lh ~/content-orchestrator/data/cookies.txt

# Verificar dentro do container
docker exec content-orchestrator ls -lh /app/data/cookies.txt

# Verificar logs ao tentar download
docker logs -f content-orchestrator
```

## Nota Importante

O arquivo de cookies precisa ter **todos** os cookies do YouTube, não apenas alguns. Um arquivo de 1619 bytes provavelmente está incompleto e não funcionará.
